# Module 2 (voice cloning) heavy dependencies.
#
# NOTE: these are now ALSO included in the main `../requirements.txt`, so a single
#   `pip install -r requirements.txt` from live_demo_of_voice_capture/ installs everything.
# This file is kept as the standalone list for a GPU-only environment.
#
# torch: install the build matching your CUDA (see https://pytorch.org). The plain wheel below
# works for CPU/most setups; swap for a CUDA-specific index if needed.
torch
voxcpm
soundfile
numpy
